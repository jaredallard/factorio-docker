name: factorio-docker
arguments:
  commands:
    - downloader
    - wrapper
  license: AGPL-3.0
  org: jaredallard
  vcs: forgejo
  vcs_host: git.rgst.io

  # Docker is handled outside of goreleaser.
  release_formats: [binaries]
modules:
  - name: go.rgst.io/rgst-io/stencil-golang
